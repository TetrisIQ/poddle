{
  "name": "poddle-relay",
  "version": "1.0.0",
  "description": "relay server for poddle.network",
  "main": "dist/Server.js",
  "repository": "https://github.com/TetrisIQ/poddle",
  "author": "TetrisIQ",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist/*",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "tsc": "tsc",
    "dev:start": "npm-run-all build start",
    "build": "npm-run-all clean tsc",
    "dev": "env $(cat .env) nodemon --watch src -e ts --exec npm run dev:start",
    "dev:example": "nodemon --watch src -e ts --exec npm run dev:start",
    "start": "node ."
  },
  "dependencies": {
    "express": "^4.17.3",
    "gun": "^0.2020.1236",
    "isomorphic-fetch": "^3.0.0",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.21",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2"
  }
}
